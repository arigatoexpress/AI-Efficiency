# CI for the AI Efficiency hub.
# Actions are SHA-pinned per this repo's supply-chain hardening convention;
# dependabot's github-actions ecosystem keeps the pins current.
name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  docs:
    name: Docs — links, claims, offline guarantees
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
      - name: Priority Metrics Intelligence checks (privacy, analytics, workflow)
        run: node --test starter-projects/priority-metrics-intelligence/test/*.test.mjs
      - name: Operations Decision Lab checks (privacy, forecasting, feasibility, workflow)
        run: node --test starter-projects/operations-decision-lab/test/*.test.mjs
      - name: Check docs (links, prompt-count claims, HTML balance, CSP)
        run: node scripts/check-docs.mjs
      - name: Prompt index in sync (prompts.json + explorer match the markdown)
        run: node scripts/build-prompt-index.mjs --check
      - name: TLH/SPH Efficiency Explorer checks (math identity, demo, CSV, offline)
        run: node starter-projects/tlh-sph-efficiency-explorer/test/run-checks.mjs
      - name: ADK shift-brief agent checks (tools, safety gate, offline guarantee)
        run: python3 starter-projects/adk-shift-brief-agent/test/run_checks.py

  app:
    name: Logistics app — install, typecheck, build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: starter-projects/fedex-logistics-intelligence-system/app
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: starter-projects/fedex-logistics-intelligence-system/app/package-lock.json
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm test
      - run: npm run build
